name: cleanup
on:
  push:
    branches:
      - master
jobs:
  test-cleanup:
    runs-on: ubuntu-20.04
    steps:
      - name: create file
        run: make normal
      
      - name: cleanup
        if: ${{ always() }}
        run: make cleanup